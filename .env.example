# Database Connection
# Get this from Supabase Dashboard → Settings → Database
# IMPORTANT: Use port 6543 (connection pooling) not 5432
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:6543/postgres?sslmode=require"

# NextAuth Secret
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your_generated_secret_here"

# Application URLs
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Huddle API (separate backend for transcripts / AI chat)
# Prefer HUDDLE_API_URL (server-side only); NEXT_PUBLIC_* also works in API routes.
HUDDLE_API_URL="https://klaar-earl.vercel.app"
# NEXT_PUBLIC_HUDDLE_API_URL="https://klaar-earl.vercel.app"

# Shared secret so backend can trust this app (required when backend uses API-key auth).
# Generate with: openssl rand -base64 32. Set the same value in the huddle backend.
# Proxy sends X-API-Key and X-User-Email; backend must accept them.
# HUDDLE_API_KEY="your_shared_secret_here"

# Google OAuth Credentials
# Get these from Google Cloud Console → APIs & Services → Credentials
GOOGLE_CLIENT_ID="your_google_client_id"
GOOGLE_CLIENT_SECRET="your_google_client_secret"
